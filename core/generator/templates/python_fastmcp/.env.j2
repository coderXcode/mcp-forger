# {{ project_name }} — MCP Server configuration
# Generated by MCP Forge — edit freely

# Base URL of the API this MCP server wraps.
# Change this to wherever your application is running.
BASE_URL={{ base_url or "http://localhost:8000" }}

# Optional: Bearer token sent as Authorization: Bearer <token>
# Leave empty if your API doesn't require auth.
API_TOKEN=
