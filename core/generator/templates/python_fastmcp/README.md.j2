# {{ project_name }} — MCP Server

Auto-generated by [MCP Forge](https://github.com/modelcontextprotocol).

## Overview

This MCP server wraps the `{{ project_name }}` API, converting its endpoints into
MCP **tools** and **resources** that LLM agents can call directly.

- **Source language:** {{ language }}
- **Framework:** {{ framework }}
- **Base URL:** `{{ base_url }}`

## Generated files

| File | Description |
|---|---|
| `server.py` | Main MCP server — tools and resources |
| `requirements.txt` | Python dependencies |

## Endpoints mapped

### Tools (mutating / action endpoints)
{% for t in tools %}
- `{{ t.operation_id }}` — `{{ t.method }} {{ t.path }}`{% if t.summary %} — {{ t.summary }}{% endif %}
{% endfor %}

### Resources (read-only endpoints)
{% for r in resources %}
- `{{ r.operation_id }}` — `{{ r.method }} {{ r.path }}`{% if r.summary %} — {{ r.summary }}{% endif %}
{% endfor %}

## Running

```bash
pip install -r requirements.txt
BASE_URL=http://your-api-host:8000 python server.py
```

## Configuration

Set these environment variables before running:

| Variable | Default | Description |
|---|---|---|
| `BASE_URL` | `{{ base_url or "http://localhost:8000" }}` | Base URL of the target API |
| `API_TOKEN` | _(empty)_ | Bearer token for authentication (if required) |
